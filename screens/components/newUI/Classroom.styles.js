import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    listItem: {
        fontSize: 24,
        fontWeight: "bold",
        color: "#000000",
    },
    subStyle: {
        fontSize: 18,
        color: "black"
    },
    container: {
        flex: 1,
        paddingTop: 22
    }
});
