import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    listItem: {
        fontSize: 32,
        fontWeight: "bold",
        color: "#000000",
    },
    subStyle: {
        fontSize: 20,
        color: "black"
    },
    container: {
        flex: 1,
        paddingTop: 22
    }
});
